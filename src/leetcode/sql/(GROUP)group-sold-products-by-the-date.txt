SELECT TO_CHAR(SELL_DATE, 'YYYY-MM-DD') AS sell_date,
COUNT(*) AS num_sold, LISTAGG(PRODUCT, ',') WITHIN GROUP (ORDER BY SELL_DATE) AS products
FROM (SELECT DISTINCT * FROM Activities)
GROUP BY SELL_DATE
ORDER BY SELL_DATE