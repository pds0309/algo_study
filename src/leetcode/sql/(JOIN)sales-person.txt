SELECT NAME
FROM SALESPERSON S
LEFT OUTER JOIN
    (
        SELECT SALES_ID
        FROM ORDERS O
        JOIN COMPANY C
        ON O.COM_ID = C.COM_ID
        WHERE C.NAME = 'RED'
    ) OC
ON S.SALES_ID = OC.SALES_ID
WHERE OC.SALES_ID IS NULL